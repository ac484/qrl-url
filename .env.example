# NOTE: Most configuration values are now hardcoded in config.py for simplicity
# This file only contains environment-specific secrets and connection strings

# Redis Configuration
# Use REDIS_URL for Redis Cloud (recommended)
REDIS_URL=redis://default:your_redis_password@your-redis-host.com:6379/0

# Option 2: Use individual parameters (fallback)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# Supabase Configuration
# Provide the project URL and the API key (service role preferred for backend usage)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_anon_or_service_role_key
# Optional: override schema or choose a different key for writes
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_SCHEMA=public
# SUPABASE_TIMEOUT=10

# MEXC API Configuration (Required)
MEXC_API_KEY=your_mexc_api_key_here
MEXC_SECRET_KEY=your_mexc_secret_key_here
MEXC_BASE_URL=https://api.mexc.com
MEXC_WS_URL=wss://wbs.mexc.com/ws
MEXC_TIMEOUT=10

# Sub-Account Configuration (Optional)
# MEXC v3 API supports two distinct sub-account systems:
# 
# 1. SPOT API (for regular users):
#    - Uses numeric subAccountId
#    - Supports universal transfers between account types (SPOT, MARGIN, ETF, CONTRACT)
#    - Available to all MEXC users
#
# 2. BROKER API (for broker/institutional accounts):
#    - Uses string subAccount name
#    - Requires special broker account permissions
#    - Provides more comprehensive sub-account management features
#
# Choose your sub-account mode:
SUB_ACCOUNT_MODE=SPOT

# For SPOT API mode (regular users):
# Provide the numeric sub-account ID
# Example: SUB_ACCOUNT_ID=123456
# SUB_ACCOUNT_ID=

# For BROKER API mode (broker accounts):
# Provide the sub-account name (string)
# Example: SUB_ACCOUNT_NAME=trading_account_001
# SUB_ACCOUNT_NAME=

# Strategy Parameters
MA_SHORT_PERIOD=7
MA_LONG_PERIOD=25
RSI_PERIOD=14
RSI_OVERSOLD=30
RSI_OVERBOUGHT=70

# Risk Control
MAX_DAILY_TRADES=5
MIN_TRADE_INTERVAL=300
STOP_LOSS_PCT=0.03
TAKE_PROFIT_PCT=0.05
MAX_POSITION_SIZE=0.3

# Position Management
CORE_POSITION_PCT=0.70
USDT_RESERVE_PCT=0.20

# ==============================================================================
# HARDCODED VALUES (no longer configurable via environment variables):
# ==============================================================================
# Flask Configuration: FLASK_ENV=production, DEBUG=False, HOST=0.0.0.0
# Trading Symbol: QRLUSDT (hardcoded - only QRL/USDT trading)
# Cache TTL: PRICE=30s, TICKER=60s, ORDER_BOOK=10s, TRADES=60s, KLINES=300s, ACCOUNT=120s, ORDERS=30s
# Logging: LOG_LEVEL=INFO, LOG_FORMAT=json
# 
# PORT is still configurable via environment variable for Cloud Run compatibility
# ==============================================================================
